---
title: Traefik
template: doc
banner:
    content: |
        ⚠️ This documentation, is under development. Expect changes!
sidebar:
    order: 1
---

import ShowCode from "@components/ShowCode.astro";
import {FileTree} from "@astrojs/starlight/components";

1. Create a folder to store the traefik data.
   In the traefik folder, create files using the following structure:

<FileTree>

- traefik
    - dynamic
        - conf.yml
    - acme.json
    - compose.yaml
    - traefik.yml

</FileTree>

In `compose.yml` put the following content in it

<ShowCode url="traefik/compose.yaml" language="yml" title="compose.yml"  isGithub />

The `traefik.yml` file should contain the following content:

<ShowCode url="traefik/traefik.yml" language="yml" title="traefik.yml" collapse="1-11,16-23,37-105,147-158" isGithub />

Once the configuration is complete, you can run the following command

```bash
docker compose up -d
```
