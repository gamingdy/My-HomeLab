---
title: Filebrowser
template: doc
---

import ReverseProxyNote from "@components/ReverseProxyNote.astro";
import {Aside} from "@astrojs/starlight/components";
import ShowCode from "@components/ShowCode.astro";

<ReverseProxyNote />

Create a folder in which to place the filebrowser data.

You can create a first file named `compose.yaml` and put the following content in it

<ShowCode url="filebrowser/compose.yaml" language="yml" title="compose.yaml" isGithub />

<Aside type="caution">
    You must create a file named `filebrowser.db` before starting
</Aside>

Once the configuration is complete, you can run the following command

```bash
docker compose up -d
```
