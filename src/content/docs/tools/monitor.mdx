---
title: Monitoring
template: doc
banner:
    content: |
        ⚠️ This documentation, is under development. Expect changes!
---

import WarnBanner from "../../../components/WarnBanner.astro";

<WarnBanner />

import ShowCode from "../../../components/ShowCode.astro";

Create a folder in which to place the monitoring tools data.

You can create a first file named `docker-compose.yml` and put the following content in it


<ShowCode url="monitor/docker-compose.yml" language="yml" title="docker-compose.yml" isGithub />

You need to create a folder named `prometheus` inside of this folder you will put the following file


<ShowCode url="monitor/prometheus/prometheus.yml" language="yml" title="prometheus.yml" isGithub />

Once the configuration is complete, you can run the following command

```bash
docker compose up -d
```
